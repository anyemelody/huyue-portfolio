# Drop AIGE assets here

Put your real AIGE files in this folder with these exact names. The site auto-uses them;
any file you don't provide is silently hidden (no broken images).

- `demo.mp4`          → the demo video (plays as the hero on the AIGE project page).
                        webm also fine — if you use demo.webm, tell me and I'll switch the path.
- `cover.png`         → a still cover (used on the Highlight card + as fallback hero).
- `architecture.png`  → your system architecture diagram (shown first in the gallery).
- `shot-01.png` … `shot-05.png` → screenshots (beauty / try-on / game / AR interaction, etc.).
                        Add as many as you have (up to shot-05 wired now; tell me if more).

Confidential note: keep model names / internal data out of these images if needed —
the page text already says "architecture shown; specific models & data are confidential."
