# Drop Sound Visualization (VFX Generation Agent) assets here

Put real files here with these names. Anything missing is silently hidden (no broken images).

- `demo.mp4`          → demo clip of the VFX Generation Agent (plays as the hero).
- `cover.png`         → still cover (Highlight card + fallback hero).
- `architecture.png`  → your multi-agent / VFX-agent system diagram (shown first).
- `effect-01.gif` … `effect-04.gif` → the 4 distinct VFX effects from one prompt.
- `hybridize.gif`     → a clip showing "motion of A + particle form of B → new effect".

WIP project — screenshots / clips of the current build are great even if rough.
(Source project lives at ~/Documents/Sound Visualization Project — I can pull frames/clips
from there directly if you connect that folder.)
